<meta http-equiv="Content-Type"
            content="text/html; charset=ISO-8859-1">
  <title>QBO Connect</title>
  <link rel="icon" href="/imgs/pageicon.png"> <!--..path corresponds to the index.js line in express ".use(express.static(path.join(__dirname, 'public')))"-->
  <link rel="stylesheet" href="/../node_modules/bulma/css/bulma.min.css"> <!--same as above line for the path-->
  <script defer src="https://use.fontawesome.com/releases/v5.0.7/js/all.js"></script>
  

  <script type="text/javascript" src="<%= locals.appCenter %>/Content/IA/intuit.ipp.anywhere-1.3.1.js"></script>
  <script>
    intuit.ipp.anywhere.setup({
      grantUrl: 'http://localhost:' + <%= locals.port %> + '/requestToken', // LOCAL, okay to ignore the <%= %> warnings
      //grantUrl: 'https://quicknode.herokuapp.com/requestToken', // PROD
      datasources: {
        quickbooks : true,  // set to false if NOT using Quickbooks API
        payments : false    // set to true if using Payments API
      }
    });
  </script>